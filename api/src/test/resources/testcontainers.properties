# Docker Desktop on Windows with WSL2 backend exposes the daemon via named pipe.
# This allows the Windows JVM (Gradle) to reach Docker even when the engine runs in WSL2.
docker.host=npipe:////./pipe/docker_engine
